source 'http://rubygems.org'
# Add dependencies required to use your gem here.
# Example:
#   gem 'activesupport', '>= 2.3.5'

# Add dependencies to develop your gem here.
# Include everything needed to run rake, tests, features, etc.
group :development do
  gem 'rspec', '~> 2.3.0'
  gem 'yard', '~> 0.6.0'
  gem 'bundler', '~> 1.0.0'
  gem 'jeweler', '~> 1.6.0'
  gem 'rcov', '>= 0'

  # Needed for development testing, but may be soft runtime dependencies.
  #gem 'activerecord', :require => 'active_record'
  #gem 'activesupport', '~> 2.3.10', :require => 'active_support'

  gem 'activerecord', '~> 3.0.7', :require => 'active_record'
  gem 'activesupport', '~> 3.0.7', :require => 'active_support'
  gem 'sqlite3-ruby', :require => 'sqlite3'

  gem 'ancestry', '~> 1.2.4'
  gem 'rack-mount', '~> 0.8.1', :require => 'rack/mount'
end
